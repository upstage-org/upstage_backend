[pytest]
# Local rootdir for unit tests. Isolates this directory from the parent
# prod_copy/upstage_backend/conftest.py, which spins up a live Postgres +
# starlette TestClient for integration tests. Unit tests should not need
# either.
testpaths = .
addopts = --confcutdir=.
